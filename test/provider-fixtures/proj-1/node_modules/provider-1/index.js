module.exports = {
  // This should not be used since xrunProvider.module points to tasks.js
  loadTasks(xrun) {
    xrun.load("provider1", {  
      "wrong-task": "echo 'This should not be loaded'"
    });
  }
}; 
